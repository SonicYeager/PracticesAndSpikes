<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Practice.Maui.ViewModels"
             x:Class="Practice.Maui.Views.SettingsPage">
    <ContentPage.BindingContext>
        <viewModels:SettingsViewModel />
    </ContentPage.BindingContext>
    
    <Grid RowDefinitions="3*,*">
        <Label Text="Options are added soon!" Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center"/>
        <VerticalStackLayout 
            Grid.Row="1" 
            VerticalOptions="Fill" 
            HorizontalOptions="Fill"
            BackgroundColor="Beige" Spacing="5">
            <Label Text="Impressum" VerticalOptions="Center" HorizontalOptions="Center"/>
            <HorizontalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Label Text="Creator: "/>
                <Label Text="SonicYeager"/>
            </HorizontalStackLayout>
            <HorizontalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Label Text="Contact: "/>
                <Label Text="kimstone.yeager@gmail.com"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Grid>
</ContentPage>