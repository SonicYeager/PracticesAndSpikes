# KubernetesCourse

Auto-generated directory index for `docker/KubernetesCourse`.

## About

Minimal Express demo for the Kubernetes course entry: `GET /` and `GET /error`
(the latter exits the process), plus Dockerfile and K8s manifests
(`deployment.yaml`, `service.yaml`).

- Stack: Node/Express, Docker, Kubernetes manifests
- Entrypoint: `app.js` (port 8080)
- Commands: `npm install`, `node app.js`; `docker build -t <name> .`; `kubectl apply -f deployment.yaml -f service.yaml`

<!-- DIRECTORY_NAVIGATION:START -->
## Directory Navigation

- Directory: `docker/KubernetesCourse`
- Parent: [`..`](..) | [Parent README](../README.md)
- Children: _None_
<!-- DIRECTORY_NAVIGATION:END -->
